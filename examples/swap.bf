s"ab"   # Adds ab string
<2      # Moves 2 backward (to start of the string)
.>.     # Prints "ab"
<       # Moves to start
;       # Swaps 'a' with 'b'
.>.     # Prints the same cells as above, but now they are "ba"